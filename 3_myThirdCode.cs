namespace MyThirdCode {
    class Program {
        public static void RunLargerValue() {
            int firstValue = 500;
            int secondValue = 600;
            int largerValue = Math.Max(firstValue, secondValue);

            Console.WriteLine(largerValue);
        }
    }
}